#compdef dn-parent-process

_pids() {
  local -a _pids
  _pids=($(ps -eo pid=))
  _values 'Process IDs' "${_pids[@]}"
}

local rc=1

_arguments -S \
  - 'help_option' \
    '(-h --help)'{-h,--help}'[Show a compact help message]' \
  - '(arguments)' \
    '*:pid:_pids' \
  && rc=0

return rc
