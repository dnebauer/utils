#! /bin/sh
# debconf script for dn-alsa-device
#
# see: debconf-devel(7), confmodule(3)

set -e
. /usr/share/debconf/confmodule  # use debconf

# check whether to remove user-created configuration files
db_input medium dn-alsa-device/delete-alsa-conf-files || true
db_go || true
