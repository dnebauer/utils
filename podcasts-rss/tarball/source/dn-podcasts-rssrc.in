# config file for dn-podcasts-rss
# ===============================

# uses key-value pairs
# space-delimited
# key must contain no spaces
# value can contain spaces and does not need to be enclosed in quotes


# base podcast directory
# ----------------------
# user will select podcast directory from a dialog
# this base directory will be stripped from the selected dirpath
#   and added to the base url defined below
# must be an absolute directory or highly unpredictable result may occur
#   -- you have been warned!
# a terminal slash is not required
base-directory /var/www/podcasts

# base url
# --------
# this is the base url to which the dirpath selected above will be added to
# a terminal slash is not required
base-url http://localhost/podcasts
